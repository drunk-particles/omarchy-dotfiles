# --- Daytime Profile (Default/Neutral) ---
# Ensures no tint is applied during daylight hours
profile {
    time     = 07:00
    identity = true
}

# --- Nighttime Profile (Warm Tint) ---
# Automatically transitions to a warmer color temperature
profile {
    time        = 18:00
    temperature = 4000
}
