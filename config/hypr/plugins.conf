source = ~/.config/hypr/colors.config

plugin {
    hyprexpo {
        columns = 2
        gap_size = 9
        gesture_distance = 100
        bg_col = rgba(1e2233cc) 
        inactive_col = rgb(2a2a33)
        active_col = rgb(3a3a44)
        border_col = rgb(ff8700)
        border_size = 3
        animation_duration = 200
        animation_fps = 60 
        workspace_method = center current # or first 1
        hyprexpo-gesture = 3, vertical, expo
        
        skip_empty = true
    }

    hyprfocus {
         mode = slide

         slide_height = 15
    }

    hyprbars {
        # Visuals
        bar_height = 29
        bar_color = $bg1
        bar_blur = true
        bar_part_of_window = true
        bar_precedence_over_border = true # Improvement: Cleaner border wrapping

        # Text
        bar_title_enabled = false
        bar_text_size = 11               # Slightly smaller for better proportions
        bar_text_font = "SF Pro Display Semibold"
        bar_text_align = center
        col.text = $fg

        # Layout
        bar_buttons_alignment = left
        bar_padding = 12
        bar_button_padding = 8
        icon_on_hover = true             # Improvement: Minimalist look until hover

        # Buttons (Correct 4-argument syntax: color, size, icon, command)
        # Using Nerd Font icons:    (Close),    (Minimize),    (Maximize)
        hyprbars-button = $red, 15,   , hyprctl dispatch killactive
        hyprbars-button = $yellow, 15,   , ~/.config/hypr/scripts/hyprbars-minimize.sh
        hyprbars-button = $green, 15,   , hyprctl dispatch togglefloating

        on_double_click = hyprctl dispatch fullscreen 1
    }
}
